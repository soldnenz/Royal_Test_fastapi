# Redis Docker Setup

Безопасная и масштабируемая конфигурация Redis в Docker для проекта Royal Test.

## 🚀 Быстрый старт

1. **Убедитесь, что Docker установлен и запущен**
2. **Запустите Redis:**
   ```bash
   start_redis.bat
   ```

## 📁 Структура файлов

```
redis/
├── docker-compose.yml      # Конфигурация Docker Compose
├── redis.conf             # Конфигурация Redis
├── start_redis.bat        # Скрипт запуска
├── stop_redis.bat         # Скрипт остановки
├── restart_redis.bat      # Скрипт перезапуска
├── status_redis.bat       # Проверка статуса
├── env_example.txt        # Пример переменных окружения
├── data/                  # Директория для данных (создается автоматически)
└── logs/                  # Директория для логов (создается автоматически)
```

## 🔧 Управление

### Запуск
```bash
start_redis.bat
```

### Остановка
```bash
stop_redis.bat
```

### Перезапуск
```bash
restart_redis.bat
```

### Проверка статуса
```bash
status_redis.bat
```

## 🔐 Безопасность

### Настройки безопасности в конфигурации:
- ✅ Пароль аутентификации
- ✅ Отключены опасные команды (FLUSHDB, FLUSHALL, DEBUG)
- ✅ Переименована команда CONFIG
- ✅ Ограничение памяти (512MB)
- ✅ Настройки сети

### Текущий пароль:
- Пароль: `Royal_Redis_1337`
- Часовой пояс: UTC+5 (Asia/Tashkent)

### Изменение пароля:
1. Откройте файл `redis.conf`
2. Найдите строку: `requirepass Royal_Redis_1337`
3. Замените пароль на нужный
4. Перезапустите Redis: `restart_redis.bat`

## 📊 Мониторинг

### Подключение к Redis CLI:
```bash
docker exec -it royal_test_redis redis-cli -a Royal_Redis_1337
```

### Просмотр логов:
```bash
docker-compose logs -f redis
```

### Проверка статистики:
```bash
docker stats royal_test_redis
```

## 🔧 Настройки производительности

### Память:
- Максимум: 512MB
- Политика: LRU (Least Recently Used)
- Автоматическая очистка при превышении лимита

### Персистентность:
- RDB: Сохранение каждые 900/300/60 секунд
- AOF: Включен с синхронизацией каждую секунду
- Гибридный режим RDB+AOF

### Сеть:
- Порт: 6379
- Максимум клиентов: 10000
- Keep-alive: 300 секунд

## 🚨 Устранение неполадок

### Redis не запускается:
1. Проверьте, что Docker запущен
2. Проверьте логи: `docker-compose logs redis`
3. Убедитесь, что порт 6379 свободен

### Проблемы с подключением:
1. Проверьте правильность пароля
2. Убедитесь, что контейнер запущен: `docker-compose ps`
3. Проверьте сетевые настройки

### Высокое потребление памяти:
1. Проверьте настройки `maxmemory` в `redis.conf`
2. Мониторьте использование: `docker stats royal_test_redis`
3. Рассмотрите увеличение лимита памяти

## 📈 Масштабирование

### Для продакшена:
1. Увеличьте `maxmemory` в `redis.conf`
2. Настройте репликацию
3. Используйте Redis Cluster для высокой доступности
4. Настройте мониторинг (Redis Exporter + Prometheus)

### Для разработки:
1. Уменьшите `maxmemory` для экономии ресурсов
2. Отключите AOF для ускорения
3. Уменьшите частоту сохранения RDB

## 🔄 Резервное копирование

Данные автоматически сохраняются в папке `data/`:
- `dump.rdb` - RDB файл
- `appendonly.aof` - AOF файл

Для ручного резервного копирования:
```bash
docker exec royal_test_redis redis-cli -a Royal_Redis_1337 BGSAVE
```

## 📝 Полезные команды Redis

```bash
# Подключение
docker exec -it royal_test_redis redis-cli -a Royal_Redis_1337

# Основные команды
INFO                    # Информация о сервере
MONITOR                 # Мониторинг команд
SLOWLOG GET 10         # Медленные запросы
MEMORY USAGE key       # Использование памяти ключа
CLIENT LIST            # Список подключенных клиентов
```

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs redis`
2. Проверьте статус: `status_redis.bat`
3. Перезапустите: `restart_redis.bat`
4. Проверьте документацию Redis: https://redis.io/documentation 
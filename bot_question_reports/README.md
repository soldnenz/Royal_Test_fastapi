# Question Reports Telegram Bot

Telegram бот для мониторинга и обработки отчетов о вопросах из MongoDB.

## Описание

Бот подключается к MongoDB и отслеживает коллекцию `question_reports`. При обнаружении отчетов со статусом `sending`, бот отправляет красиво оформленные сообщения в указанный Telegram канал и обновляет статус на `pending`.

## Технологии

- **Python 3.11+**
- **aiogram 3.4.1** - Telegram Bot API
- **pymongo 4.6.1** - MongoDB драйвер
- **pydantic 2.5.2** - Валидация данных
- **python-dotenv 1.0.0** - Управление переменными окружения

## Структура проекта

```
bot_question_reports/
├── main.py              # Основной файл запуска
├── config.py            # Конфигурация
├── database.py          # Работа с MongoDB
├── models.py            # Модели данных
├── telegram_bot.py      # Telegram API (aiogram)
├── requirements.txt     # Зависимости
├── Dockerfile          # Docker конфигурация
├── docker-compose.yml  # Docker Compose
├── env.example         # Пример переменных окружения
├── start.bat           # Запуск локально
├── start_docker.bat    # Запуск через Docker
├── stop_docker.bat     # Остановка Docker
├── logs.bat            # Просмотр логов
└── README.md           # Документация
```

## Установка и запуск

### Локальный запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Запустите бота:
```bash
python main.py
```

Или используйте bat файл в Windows:
```bash
start.bat
```

### Запуск через Docker

1. Сборка и запуск:
```bash
docker-compose up -d
```

Или используйте bat файл в Windows:
```bash
start_docker.bat
```

2. Просмотр логов:
```bash
docker-compose logs -f
```

Или используйте bat файл в Windows:
```bash
logs.bat
```

3. Остановка:
```bash
docker-compose down
```

Или используйте bat файл в Windows:
```bash
stop_docker.bat
```

## Конфигурация

### Переменные окружения

- `MONGO_URI` - URI подключения к MongoDB
- `MONGO_DB_NAME` - Название базы данных
- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота
- `TELEGRAM_CHAT_ID` - ID чата для отправки сообщений
- `TELEGRAM_WARNING_TOPIC` - ID топика в чате
- `POLLING_INTERVAL` - Интервал проверки новых отчетов (в секундах, по умолчанию 60)

### Структура отчета

```json
{
  "_id": "686c09b842c7d2a62edd8c02",
  "lobby_id": "2GQ1EP",
  "question_id": "686becc52d8515fec16840de",
  "user_id": "684ec5857dfc24690c8ecbcc",
  "report_type": "technical_error",
  "description": "Описание проблемы",
  "status": "sending",
  "created_at": "2025-07-07T17:54:00.439+00:00",
  "ip_address": "212.96.79.182"
}
```

## Функциональность

1. **Мониторинг**: Бот каждую минуту проверяет новые отчеты со статусом `sending`
2. **Отправка сообщений**: Форматирует и отправляет отчеты в Telegram канал
3. **Обновление статуса**: После успешной отправки меняет статус на `pending`
4. **Логирование**: Ведет подробные логи всех операций
5. **Обработка ошибок**: Корректно обрабатывает ошибки подключения и отправки

## Типы отчетов

- `technical_error` - Техническая ошибка 🔧
- `content_error` - Ошибка в контенте 📝
- `inappropriate_content` - Неподходящий контент ⚠️
- `other` - Другое ❓

## Логи

Логи сохраняются в файл `bot.log` и выводятся в консоль.

## Особенности

- Использует **aiogram 3.4.1** для работы с Telegram API
- Интервал опроса БД: **60 секунд (1 минута)**
- Поддержка Docker для легкого развертывания
- Автоматический перезапуск при сбоях
- Красивое форматирование сообщений с эмодзи 